---
import Layout from "../layouts/Layout.astro";
import ForgotPasswordForm from "../components/ForgotPasswordForm.tsx";
import { ToastProvider } from "../components/ToastProvider.tsx";
---

<Layout title="Reset hasła - FairPlay">
  <main class="min-h-screen bg-muted/40 flex items-center justify-center px-4 py-16">
    <div class="w-full max-w-md space-y-6">
      <section class="rounded-xl border border-border bg-background shadow-sm">
        <header class="px-6 pt-6 text-center space-y-2">
          <p class="text-xs font-medium uppercase tracking-wide text-primary/80">Odzyskaj dostęp</p>
          <h1 class="text-3xl font-semibold text-foreground">Zresetuj hasło</h1>
          <p class="text-sm text-muted-foreground">
            Wprowadź adres email powiązany z kontem, a wyślemy link do ustawienia nowego hasła.
          </p>
        </header>
        <div class="px-6 pb-6 pt-4">
          <ForgotPasswordForm client:load />
        </div>
      </section>
      <p class="text-center text-xs text-muted-foreground">
        Nie pamiętasz adresu email konta? Skontaktuj się z administratorem FairPlay, aby odzyskać dostęp.
      </p>
    </div>
  </main>
  <ToastProvider client:load />
</Layout>

