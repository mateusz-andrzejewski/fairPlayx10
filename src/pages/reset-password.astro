---
import Layout from "../layouts/Layout.astro";
import ResetPasswordForm from "../components/ResetPasswordForm.tsx";
import { ToastProvider } from "../components/ToastProvider.tsx";
---

<Layout title="Resetowanie hasła - FairPlay">
  <main class="min-h-screen bg-muted/40 flex items-center justify-center px-4 py-16">
    <div class="w-full max-w-md space-y-6">
      <section class="rounded-xl border border-border bg-background shadow-sm">
        <header class="px-6 pt-6 text-center space-y-2">
          <p class="text-xs font-medium uppercase tracking-wide text-primary/80">Ustaw nowe hasło</p>
          <h1 class="text-3xl font-semibold text-foreground">Resetowanie hasła</h1>
          <p class="text-sm text-muted-foreground">
            Wprowadź nowe, bezpieczne hasło dla swojego konta FairPlay.
          </p>
        </header>
        <div class="px-6 pb-6 pt-4">
          <ResetPasswordForm client:load />
        </div>
      </section>
      <p class="text-center text-xs text-muted-foreground">
        Pamiętaj, aby wybrać silne hasło zawierające wielkie litery, małe litery i cyfry.
        Link resetowania wygaśnie po 60 minutach.
      </p>
    </div>
  </main>
  <ToastProvider client:load />
</Layout>
