name = "fairplayx10"
compatibility_date = "2024-11-13"

# Pages configuration
pages_build_output_dir = "./dist"

# KV Namespaces for sessions
# Note: You'll need to create these bindings in Cloudflare Dashboard
# or via wrangler CLI before deploying
[[kv_namespaces]]
binding = "SESSION"
id = "" # Add your KV namespace ID here after creating it

# Environment variables
# Set these in Cloudflare Pages dashboard under Settings > Environment variables
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY (if needed for server-side operations)

